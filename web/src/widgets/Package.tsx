import "./Package.css";

const Package = () => {
  return (
    <button className="btn" onClick={() => {}}>
      <div className="package selected">some text</div>
    </button>
  );
};

export default Package;
